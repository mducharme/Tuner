/**
* Unit tests for Tuner
*/
QUnit.module("Tuner");

// Simple test to ensure everything has been included properly
QUnit.test("Tuner class", function(assert) {
	assert.ok(Tuner, "Main namespace exists");
});
